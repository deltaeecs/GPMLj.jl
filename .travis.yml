language: julia

os:
  - linux
  - osx

julia:
  - 1.0
  - nightly

addons:
  apt:
    packages:
      - python3-gpflow==1.5.1

env:
  matrix:
    - PYTHON=python3

codecov: true

coveralls: true

matrix:
  allow_failures:
    - julia: nightly
  fast_finish: true

notifications:
  email: false